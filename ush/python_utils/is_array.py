#!/usr/bin/env python3

def is_array(var):
    """ Checks if variable is an instance of List

    Args:
        var: the variable
    Returns:
        True if variable is a list
    """
    if type(var) == list:
        return True
    return False

